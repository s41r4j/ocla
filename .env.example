# Required for /api/submit + /api/leaderboard
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"

# Vercel Postgres note:
# If you add Vercel Postgres, Vercel provides `POSTGRES_PRISMA_URL`.
# You can set DATABASE_URL to that value (or just set POSTGRES_PRISMA_URL and the app will use it).
